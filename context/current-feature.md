# Current Feature

<!-- Feature name and short description -->

## Status

<!-- Not Started | In Progress | Complete -->

## Goals

<!-- Goals and requirements -->

## Notes

<!-- Any extra notes -->

## History

<!-- Keep this updated. Earliest to latest -->

- **2026-08-21** — Item Drawer completed on `feature/item-drawer` per `context/features/item-drawer-spec.md`. Added a right-side shadcn `Sheet` drawer (`src/components/items/ItemDrawer.tsx`) that opens when an `ItemCard` is clicked and fetches full item detail on click via a new auth-checked `GET /api/items/[id]` route, backed by `getItemDetail(userId, itemId)` in `src/lib/db/items.ts` (scoped by user, 404s for cross-user access). Added `ItemDrawerProvider` (`src/components/items/ItemDrawerProvider.tsx`), a client context wrapping both `dashboard/layout.tsx` and `items/[type]/layout.tsx` so any page inside the app shell can open the drawer by item ID. `ItemCard` converted from a `next/link` navigation to a client component that opens the drawer instead — no more separate item page, matching the spec. The drawer shows a skeleton while loading, then title/type/description/content (plain `<pre>` block, no code editor — explicitly deferred per spec)/url/tags/collections/dates, plus an action bar. Scope of the action bar was clarified with the user mid-build: Favorite and Pin are fully functional (new `src/actions/items.ts` server actions, ownership-checked, with optimistic UI + rollback on failure), Copy copies content/url to the clipboard, and Edit/Delete render disabled since no edit form or delete-confirmation flow exists yet. During review, added `router.refresh()` after a successful Favorite/Pin toggle so the underlying server-rendered list (stats, Pinned section membership, card star badges) stays in sync with the drawer instead of going stale until a manual reload — caught live by pinning an item in the drawer and observing it appear in the dashboard's Pinned section without a reload. Also removed `ItemSummary.collectionId` (and the Prisma sub-queries computing it) from `getDashboardItems`/`getItemsByType`, since it became dead code once `ItemCard` stopped using it for its old link `href`. Added `src/actions/items.test.ts` (7 Vitest unit tests for `toggleItemFavorite`/`toggleItemPinned`, mirroring the existing `profile.test.ts` mock pattern) — `getItemDetail` and the API route were left untested, consistent with this repo's existing convention of not unit-testing thin Prisma-query functions or route handlers. A leftover dev server from a prior session (unrelated to this feature) was found mid-verification giving inconsistent sign-in results; killed and restarted with the user's explicit approval before continuing. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (25/25 passing), and a live Playwright + Neon MCP pass: drawer opens on both `/dashboard` (Pinned/Recently used sections) and `/items/links`; favorite/unfavorite and pin/unpin persisted to and reverted in Neon; Copy worked with no console errors; a URL-type item (`shadcn/ui`) rendered its link correctly. No test data left in Neon.

- **2026-08-21** — Items List View completed on `feature/items-list-view` per `context/features/item-list-view-spec.md`. Added the dynamic route `/items/[type]` (`src/app/items/[type]/page.tsx` + `layout.tsx` mirroring the dashboard's `SidebarProvider`/`AppSidebar`/`TopBar` shell, same pattern as `src/app/dashboard/layout.tsx`). Added `getItemsByType(userId, typeSlug)` to `src/lib/db/items.ts`, resolving the slug against `getSystemItemTypes()` and returning `null` (→ `notFound()`) for unknown slugs; scoped by `userId` from the start, matching the 2026-07-30 per-user-scoping convention. Extracted the sidebar's inline `${type.name.toLowerCase()}s` slug logic into a shared `itemTypeSlug()` helper, now used by both `AppSidebar` and the new page. The page reuses the existing `ItemCard` component and `itemTypeIconMap` (with the same inline `?? Box` fallback convention `ItemCard` uses, to satisfy the repo's `react-hooks/static-components` ESLint rule) — zero new UI components needed. Grid is `grid-cols-1 md:grid-cols-2` per spec. Added `/items/:path*` to `src/proxy.ts`'s matcher so the route is session-gated like `/dashboard` and `/profile`. Added 2 `Note`-type items to `prisma/seed.ts` (`Pre-deploy checklist` in DevOps, `Custom hook naming guidelines` in React Patterns) at the user's request mid-build, so a previously-empty item type (no seed data existed for `Note`) had real data to verify the page against instead of only exercising the empty state. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright pass: `/items/snippets` (existing data, correct blue border/icon), `/items/notes` (newly seeded, correct yellow border/icon), `/items/links` at a 414px mobile viewport (confirmed single-column, 2-column at desktop width), and `/items/bogus` (confirmed 404). Noted but left as-is: `/items/[type]/layout.tsx` duplicates `/dashboard/layout.tsx` verbatim — no shared app-shell abstraction exists yet in this codebase (`/profile` opts out of the shell entirely), so extracting one for a single second consumer was judged to be scope creep beyond this feature's goals.

- **2026-08-21** — Fixed both Critical findings from the 2026-08-17 auth security audit, on `fix/critical-auth-findings`. `src/lib/url.ts`'s `getBaseUrl()` no longer reads `x-forwarded-host`/`host` from the request at all — it now returns a fixed `APP_URL` env var (falling back to `http://localhost:3000`), closing the Host Header Injection → password-reset-poisoning vector (Critical #1). Added `APP_URL="https://devstash-lilac-eta.vercel.app"` to `.env.production` (user-provided; gitignored, untracked) and documented the var in `.env.example`. For Critical #2 (pre-account-takeover via unverified Credentials signup + `allowDangerousEmailAccountLinking`), removed `allowDangerousEmailAccountLinking: true` from the GitHub provider in `src/auth.ts` (now just `GitHub`) and added an `emailVerified` check to the Credentials `authorize()` — selects `emailVerified` in the Prisma lookup and throws a new `EmailNotVerifiedError` (extends `CredentialsSignin`, `code: "email_not_verified"`) when a password-valid user hasn't verified their email. `signInWithCredentials` (`src/actions/auth.ts`) catches this specific error before the generic `AuthError` branch and returns a distinct "Please verify your email..." message, reusing the existing `Alert`-based error UI with zero component changes. This explicitly reverses the 2026-07-29 decision to leave `authorize()` ungated — the seeded demo user (`demo@devstash.io`) already has `emailVerified` set in `prisma/seed.ts` so it's unaffected. Also dropped two now-redundant `await`s on the newly-synchronous `getBaseUrl()` (`src/actions/auth.ts`, `src/app/api/auth/register/route.ts`). Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright + Neon MCP pass: demo account still signs in and reaches `/dashboard` unaffected; a freshly registered account correctly blocked at sign-in with the new "please verify your email" message while unverified; verifying via the emailed token then signing in again succeeds; grepped `src/` afterward to confirm zero remaining references to `x-forwarded-host`/request `Host` headers anywhere in the codebase. Test account and its verification token deleted from the dev branch afterward. Only the 2 Critical findings were addressed — the report's High/Medium/Low findings remain open for a future pass.

- **2026-06-03** — Initial Next.js 16.2.6 project scaffolded via Create Next App. Stack includes React 19, TypeScript (strict), Tailwind CSS v4, Prisma 7 + Neon, NextAuth v5, Cloudflare R2, OpenAI, shadcn/ui, and Stripe. Project context, CLAUDE.md, and coding standards added.
- **2026-06-13** — Dashboard UI Phase 1 of 3 completed. Initialized ShadCN UI, added base components, created the /dashboard route with dark mode by default, a top bar (DevStash branding, sidebar toggle, search, "New collection" and "New item" buttons), and placeholder Sidebar/Main sections.
- **2026-06-15** — Dashboard UI Phase 2 of 3 completed. Added a collapsible sidebar (icon-collapse on desktop, drawer on mobile) with Dashboard/Favorites/Recent nav, an Item Types section linking to /items/[type] (with PRO badges on Files/Images), Favorite and Recent collections sections from mock data, and a user avatar footer with Upgrade to Pro CTA. Moved branding out of the top bar and wired the sidebar toggle to the new SidebarTrigger.
- **2026-06-15** — Dashboard UI Phase 3 of 3 completed. Built the main dashboard content area on `feature/dashboard-phase-3`: a "Welcome back" header with item/collection totals, 4 stats cards (items, collections, favorite items, favorite collections), a Collections section (cards with favorite star, item-type icons, item count, last-used time), a Pinned items section, and a Recently used section (10 items). Added shared `ItemCard`/`CollectionCard` components, a `formatRelativeTime` helper, a shared `itemTypeIconMap`, and the shadcn `Card` component. Refactored `AppSidebar` to use the shared icon map. Verified with `npm run lint`, `tsc --noEmit`, dev server render, and `npm run build`.
- **2026-06-16** — Started Neon PostgreSQL + Prisma 7 setup. Feature spec documented in `context/features/database-spec.md`.
- **2026-06-16** — Implemented Neon + Prisma 7 on `feature/database-setup`. Installed `prisma@7`, `@prisma/client`, `@prisma/adapter-neon`, `@neondatabase/serverless`, `dotenv`, `tsx`. Key Prisma 7 breaking changes applied: `provider = "prisma-client"` with mandatory `output` in the generator, `url`/`directUrl` removed from `datasource` block (now live in `prisma.config.ts` and the adapter). Wrote `prisma.config.ts` (dotenv loads `.env.local` + `.env`, falls back `DIRECT_URL → DATABASE_URL` for CLI). Wrote `src/lib/prisma.ts` singleton with `PrismaNeon` adapter and hot-reload guard. Wrote `prisma/seed.ts` for 7 system ItemTypes with stable IDs. Created `.env.example`. Added `db:generate`, `db:migrate`, `db:deploy`, `db:seed`, `db:studio` scripts. Ran `prisma migrate dev --name init` — migration `20260616201744_init` created and applied to Neon dev branch. Ran `prisma db seed` — all 7 system ItemTypes inserted. Verified `prisma generate`, `tsc --noEmit`, and `eslint` all pass clean.
- **2026-06-24** — Rewrote `prisma/seed.ts` on `feature/seed-data` per `context/features/seed-spec.md`. Added `bcryptjs` + `@types/bcryptjs`. Seed now upserts: the 7 system ItemTypes (unchanged), a demo user (`demo@devstash.io`, password hashed with bcryptjs at 12 rounds, `isPro: false`), 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources), and 18 items across them (3 snippets, 3 prompts, 1 snippet + 1 command + 2 links, 4 commands, 4 links — links use real URLs) joined via `ItemCollection`. All records use stable IDs so the seed is idempotent. Verified row counts after a fresh run and a re-run (1 user, 5 collections, 18 items, 18 item-collection links, 7 item types — no duplicates), plus `tsc --noEmit`, `npm run lint`, and `npm run build` all pass clean.
- **2026-07-14** — Dashboard Collections completed on `feature/dashboard-collections` per `context/features/dashboard-collections-spec.md`. Added `src/lib/db/collections.ts` with `getCollectionsWithStats()`, querying the demo user's collections via Prisma and computing item count, most-recent activity, and per-type usage counts to derive a dominant item type. `src/app/dashboard/page.tsx` is now an async server component that fetches real collections directly (items/pinned/recent sections remain on mock data, unchanged). `CollectionCard` now shows a colored left border driven by the dominant item type (matching the existing `ItemCard` convention) plus icons for every type present in the collection; `CollectionsSection` gained a "Color-coded by dominant item type" subtitle. `formatRelativeTime` now accepts `Date | string` for real DB timestamps. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a headless-Chrome screenshot of `/dashboard` confirming the 5 real DB collections render with correct names, item counts, border colors, and type icons.
- **2026-07-14** — Dashboard Items completed on `feature/dashboard-items` per `context/features/dashboard-items-spec.md`. Added `src/lib/db/items.ts` with `getDashboardItems()`, a single Prisma query (items + type + tags + collections) that derives total/favorite counts, pinned items, and the 10 most recently used items sorted by `lastUsedAt ?? updatedAt`. `src/app/dashboard/page.tsx` now fetches collections and items in parallel and no longer imports mock items. `ItemCard`/`PinnedItemsSection`/`RecentItemsSection` now take the real `ItemSummary` shape instead of mock `Item`. Removed the now-unused `Item`/`ContentType` types and `items` array from `src/lib/mock-data.ts`. Enriched `prisma/seed.ts` with 24 tags (2 per item via a new `ItemTag` upsert helper), staggered `lastUsedAt` timestamps per item, and 2 pinned items (`useDebounce hook`, `Code review prompt`) — the prior seed left every item with null `lastUsedAt`, no tags, and `isPinned: false`, which would have made the pinned section and tag badges impossible to verify. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a headless-Chrome screenshot of `/dashboard` confirming 18 real items, correct pinned/recent sections, tag badges, and type-colored borders/icons.
- **2026-07-14** — Stats & Sidebar completed on `feature/stats-sidebar` per `context/features/stats-sidebar-spec.md`. Dashboard stats already read from the DB as of the prior two features, so no change was needed there. Added `getSystemItemTypes()` to `src/lib/db/items.ts` (returns `isSystem` item types, sorted to a fixed display order since DB row order isn't guaranteed). `AppSidebar` is now an async server component that fetches real item types (`getSystemItemTypes()`) and collections (`getCollectionsWithStats()`, already used by the dashboard page) instead of `mock-data`. Item Types section links to `/items/[typename]` with real icons/colors; PRO badge check now keys off type name (`File`/`Image`) since DB ids differ from the old mock ids. Recent collections show a colored circle (from `dominantColor`) instead of the clock icon; a "View all collections" link to `/collections` was added under the Recent list. Removed the now-unused `itemTypes`/`Collection` mock exports from `src/lib/mock-data.ts`, keeping only `currentUser`. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and headless-Chrome screenshots of `/dashboard` confirming real item types/icons, an empty Favorites section (no DB collections are favorited yet, which is correct), colored-circle recents, and the new "View all collections" link.
- **2026-07-18** — Checked Pro Badge on Sidebar completed on `feature/pro-badge-sidebar-check` per `context/features/pro-badge-sidebar-check.md`. Pure audit task — verified the PRO badge on Files/Images in `AppSidebar` (`<Badge variant="secondary" className="ml-auto">PRO</Badge>`) already used the ShadCN `Badge` component, was clean/subtle (secondary variant, small pill), and had "PRO" in all uppercase. All three requirements were already met, so no source code changes were made. Verified with `npm run build`.
- **2026-07-19** — Audit quick-wins cleanup completed on `feature/audit-quick-wins`, addressing the low-risk subset of the 2026-07-18 code-scanner audit (1 High, 4 Medium, 3 Low findings). Extracted the duplicated `DEMO_USER_ID` into `src/lib/constants.ts`. Wrapped `getCollectionsWithStats()`, `getSystemItemTypes()`, and the new `getDashboardItems()`/`getCurrentUser()` in React's `cache()` so the sidebar layout and dashboard page share one query instead of running it twice per render. Added `select` projections to all dashboard Prisma queries to stop over-fetching unused columns (`content`, `fileUrl`, `url`, `description`). Added `orderBy: { addedAt: "asc" }` to the `collections` include in `getDashboardItems()` so `item.collections[0]` is deterministic. Added `getItemTypeIcon()` (falls back to `Box`) to `src/lib/item-type-icons.tsx` and used it in `CollectionCard`/`AppSidebar`; `ItemCard` uses an equivalent inline `itemTypeIconMap[...] ?? Box` lookup instead of a helper call, since the repo's `react-hooks/static-components` ESLint rule flags a function call assigned to a capitalized variable at a component's top level (but not inside a `.map()` closure) as "creating a component during render." `formatRelativeTime` now clamps future/negative diffs to "just now". Added a comment documenting why `CollectionCard` intentionally keeps the default (non-`sm`) `Card` size. Added `src/lib/db/user.ts` with `getCurrentUser()`, replacing the mismatched mock "Alex Doe" identity in `AppSidebar`/`DashboardPage` with the real seeded "Demo User" (`mock-data.ts` itself was left in place, unused, for future reuse per user request). Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live `/dashboard` check via `npm run dev` + curl (no headless browser tooling available in this environment) confirming real seeded collections/items and the corrected user identity render with no server errors. Separately surfaced (not fixed, out of scope): `npm run start` reads `.env.production`, which points at an unseeded Neon branch different from dev's, and `/dashboard` is statically prerendered at build time rather than rendered per-request — both pre-existing issues unrelated to this feature.
- **2026-07-24** — Auth Setup Phase 1 of 3 completed on `feature/auth-phase-1` per `context/features/auth-phase-1-spec.md`. Installed `next-auth@5.0.0-beta.32` and `@auth/prisma-adapter`. Added the split edge-compatible config pattern: `src/auth.config.ts` (GitHub provider only) and `src/auth.ts` (`PrismaAdapter`, `session: { strategy: "jwt" }`, `jwt`/`session` callbacks exposing `session.user.id`). Added `src/app/api/auth/[...nextauth]/route.ts` exporting the `GET`/`POST` handlers, `src/proxy.ts` protecting `/dashboard/:path*` and redirecting unauthenticated requests to NextAuth's default `/api/auth/signin` (with `callbackUrl` preserved), and `src/types/next-auth.d.ts` extending `Session.user` with `id`. Used Context7 to confirm the current NextAuth v5 split-config and proxy patterns before implementing, per spec instructions. The `User`/`Account`/`Session`/`VerificationToken` Prisma models already existed from the original schema, so no migration was needed. Added `AUTH_SECRET` to `.env` (existing `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` were reused as-is; `.env.example` already documented all three). Verified with `tsc --noEmit`, `npm run lint`, `npm run build` (confirms `ƒ Proxy (Middleware)` registered), and a live `npm run dev` + curl check: `/dashboard` returns a 302 to `/api/auth/signin?callbackUrl=...`, the default sign-in page renders the GitHub button, and unrelated routes (`/`) are unaffected by the proxy matcher. Full GitHub OAuth round-trip was not exercised (no browser/OAuth callback available in this environment) — full sign-in flow should be manually verified in-browser before Phase 2.
- **2026-07-24** — Auth Setup Phase 2 of 3 completed on `feature/auth-phase-2` per `context/features/auth-phase-2-spec.md`. Added a Credentials (email/password) provider alongside GitHub: `src/auth.config.ts` gets an Edge-safe `authorize: () => null` placeholder (bcrypt/Prisma aren't Edge-compatible, and this config is imported by `src/proxy.ts`), and `src/auth.ts` overrides `providers` with the real implementation — Zod-validated input, `prisma.user.findUnique` scoped to only the fields needed, `bcrypt.compare` against the stored hash, returning just `{id, name, email, image}` so the password hash never reaches the JWT/session. The `password` field already existed on `User` from the Phase 1 migration, so no new migration was needed. Added `POST /api/auth/register` (`src/app/api/auth/register/route.ts`): Zod-validates `name`/`email`/`password`/`confirmPassword` (including a `.refine` password-match check), checks for an existing user (409 on conflict), hashes with `bcryptjs` at 12 rounds (matching the seed convention), creates the user, and returns `{success, data/error}`. Added `src/lib/validations/auth.ts` (`signInSchema`, `registerSchema`) and installed `zod` (not previously a dependency) after confirming the v4 API via Context7. Used Context7 to confirm the current NextAuth v5 Credentials/`authorize` pattern and Zod v4's `safeParse`/`issues` shape before implementing. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live `npm run dev` + curl run covering the full spec test plan: registration success/duplicate-email (409)/password-mismatch (400)/invalid-email (400), credentials sign-in producing a correct session (`id`/`name`/`email`/`image`, no password leak), `/dashboard` still gated by the proxy for unauthenticated requests, and the GitHub button still rendering on `/api/auth/signin` (Phase 1 regression check). Custom sign-in/register UI and wiring the signed-in user's identity into the dashboard/sidebar (replacing `DEMO_USER_ID`) are deferred to Phase 3 per `context/features/auth-phase-3-spec.md`. Excluded `.mcp.json` (contains a live API key) and `.playwright-mcp/`/`.claude/agent-memory/`/`.claude/agents/` (local tooling artifacts, unrelated to this feature) from the commit.
- **2026-07-25** — Favorite items & collections seed data completed on `feature/favorite-seed-data`. Gave `seedCollection()` an optional `isFavorite` param (defaulting `false`) and marked React Patterns and Design Resources as favorite collections; added `isFavorite: true` to 3 items across different collections/types (useLocalStorage hook, Documentation generation prompt, shadcn/ui). Re-ran `prisma db seed` against the dev branch; confirmed the flags landed via a direct Neon MCP SQL query. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright sign-in + `/dashboard` screenshot confirming stats cards read "Favorite items 3" / "Favorite collections 2", star badges render on both favorited collection cards, the sidebar's Favorites nav section now lists both collections (previously empty per the 2026-07-14 Stats & Sidebar entry), and the favorited item shows its star in the Recently used list.
- **2026-07-25** — Auth Setup Phase 3 of 3 completed on `feature/auth-phase-3` per `context/features/auth-phase-3-spec.md`. Replaced NextAuth's default sign-in page with custom UI. Added `src/app/sign-in/page.tsx` (server component reading `callbackUrl` from `searchParams`) rendering a client `SignInForm` (`useActionState` + a `signInWithCredentials` server action in the new `src/actions/auth.ts`, inline error display via the newly-added shadcn `Alert` component) plus a GitHub button backed by `signInWithGitHub`. Added `src/app/register/page.tsx` (client form posting to the existing `/api/auth/register`, redirects to `/sign-in` on success). Added `pages: { signIn: "/sign-in" }` to `src/auth.config.ts` and repointed `src/proxy.ts`'s unauthenticated redirect from NextAuth's default `/api/auth/signin` to `/sign-in`. Added a reusable `UserAvatar` component (`src/components/auth/UserAvatar.tsx`, GitHub `image` or initials fallback via a new `getInitials()` in `src/lib/format.ts`) and a minimal `/profile` page, now included in the proxy's protected matcher alongside `/dashboard/:path*`. Rewrote `getCurrentUser()` (`src/lib/db/user.ts`) to read the real session via `auth()` instead of the hardcoded `DEMO_USER_ID`, and to select `image`; dashboard *content* queries (`collections.ts`/`items.ts`) intentionally still scope to the seeded demo user — only identity/sidebar wiring was in scope for this phase. `AppSidebar`'s footer now renders `UserAvatar` + name inside a Base UI `DropdownMenu` (installed shadcn `label`/`alert`, no new npm deps) showing a `DropdownMenuLabel` header row (avatar, name, email — this required wrapping the label in a `DropdownMenuGroup`, since Base UI's `MenuPrimitive.GroupLabel` throws "MenuGroupContext is missing" outside one, a gap in Base UI's dropdown-menu.tsx not caught until a live Playwright pass), a "Profile" link, and a "Sign out" item whose `render` prop is a `<form action={signOutAction}>` (server action calling NextAuth's `signOut`). Verified with `tsc --noEmit`, `npm run lint`, `npm run build` (as a side effect, `/dashboard` and `/profile` are now dynamically rendered instead of static-prerendered, resolving a staleness issue flagged in the 2026-07-19 audit entry), and a full live Playwright pass: custom `/sign-in` render, credentials sign-in, inline "Invalid email or password" error on bad credentials, register → duplicate-email 409, a fresh register → sign-in → dashboard round trip (test account deleted from the dev branch afterward via Neon MCP with explicit user confirmation), sidebar dropdown showing the real user's avatar/name/email, Profile navigation, Sign out → redirect to `/sign-in`, and the proxy re-gating `/dashboard` post-sign-out. GitHub OAuth button renders but the full OAuth round-trip wasn't exercised (no live GitHub callback in this environment, consistent with Phase 1/2 notes).
- **2026-07-29** — Email Verification on Register completed on `feature/email-verification-on-register`. Installed `resend`. Added `src/lib/resend.ts` (client + `EMAIL_FROM`, defaults to `onboarding@resend.dev`, overridable via `RESEND_FROM_EMAIL`), `src/lib/db/verification.ts` (`createVerificationToken()`/`consumeVerificationToken()`, reusing the existing unused `VerificationToken` model — one active 24h token per email, deleting any prior token for that identifier on reissue), and `src/lib/email/send-verification-email.ts`. `POST /api/auth/register` now issues a token and emails the verify link after user creation; registration still succeeds even if the send fails (logged server-side, `emailSent: false` returned to the client). Added `src/app/verify-email/page.tsx`, an async server component that consumes `?token=` directly via Prisma (no separate API route) and renders success/expired/invalid. `src/app/register/page.tsx` now shows an inline "Check your email" panel instead of redirecting to `/sign-in`. `src/auth.ts` gained a `signIn` callback that auto-sets `emailVerified` for GitHub OAuth users on sign-in (GitHub already verifies email; avoids ever prompting them). Scope was corrected mid-build: the spec's open question about gating credentials sign-in on `emailVerified` was initially resolved as "block," then explicitly reversed by the user to keep sign-in ungated so seeded/test accounts keep working — `src/auth.ts`'s `authorize()` was left untouched. Used Context7 to confirm the current Resend Node SDK send pattern before implementing. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright + Neon MCP pass: register → real Resend send confirmed (`emailSent: true` using Resend's `delivered@resend.dev` test recipient) → "Check your email" panel; valid token → "Email verified" and `User.emailVerified` set in the dev DB; token single-use (revisiting the same token afterward → "Invalid link"); expired token (manually backdated in Neon) → "Link expired"; unrecognized token → "Invalid link"; a freshly registered unverified user still signs in and reaches `/dashboard` uncontested. Both test accounts and their tokens/sessions deleted from the dev branch afterward via Neon MCP with explicit user confirmation. GitHub OAuth auto-verify was not exercised live (no OAuth callback available in this environment, consistent with prior auth-phase notes).
- **2026-07-29** — Fail Registration When Verification Email Can't Be Sent, completed on `feature/verify-email-send-failure-ui`. This reversed the prior feature's premise mid-build: originally the ask was to surface a send failure in the UI without blocking account creation (built and verified same-day); the user then explicitly flipped it so a failed send blocks the account entirely. Reordered `POST /api/auth/register` (`src/app/api/auth/register/route.ts`) to create the verification token and send the email *before* creating the `User` row — `VerificationToken.identifier` has no FK to `User`, so this ordering is valid. On send failure: returns `502` with `{ success: false, error }` and creates no user row; on success: unchanged (hash password → create user → 201). `src/app/register/page.tsx` reverted the just-added `RegisteredState`/`emailSent` branch back to the simpler single-error-on-the-form UX (failed sends now show in the existing `Alert`, same as duplicate-email/validation errors). Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright + Neon MCP pass: a send failure (Resend sandbox rejecting a non-test domain) leaves the user on the form with the error and zero `User` rows created (confirmed via Neon and a direct curl request returning `502`); a successful send (Resend's `delivered@resend.dev`) still creates the account and shows "Check your email". Test account cleaned up from the dev branch afterward with explicit user confirmation.
- **2026-07-30** — Scope Dashboard Data to the Signed-In User completed on `feature/scope-data-to-user`. `getDashboardItems()` (`src/lib/db/items.ts`) and `getCollectionsWithStats()` (`src/lib/db/collections.ts`) now take a required `userId` parameter and filter by it instead of the hardcoded `DEMO_USER_ID`; both are still wrapped in React `cache()`, now correctly memoized per-user rather than globally. `src/app/dashboard/page.tsx` and `src/components/dashboard/AppSidebar.tsx` already called `getCurrentUser()` for display purposes — both now await it first and pass `currentUser.id` into the two data queries instead of running everything in one undifferentiated `Promise.all`. Deleted `src/lib/constants.ts` (`DEMO_USER_ID`) as dead code — its only two references were the ones just removed, and `prisma/seed.ts` already carries its own local copy for seeding. No schema/migration changes needed since `Item.userId`/`Collection.userId`/`Tag.userId` were already required FKs. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright + Neon MCP pass: signed in as the seeded `demo@devstash.io`, dashboard/sidebar still show all 18 items, 5 collections, and both favorited collections exactly as before (proving the demo account isn't special-cased, it just owns that data); registered and signed in as a brand-new user, whose dashboard correctly showed "0 items across 0 collections" with empty Favorites/Recent sections and no seed data leaking through. Test account cleaned up from the dev branch afterward. Flagged but explicitly out of scope: there's still no in-app create/update/delete flow for items or collections anywhere in the app, so new users have no way yet to populate their own (now correctly empty) dashboard — a separate future feature. Also deleted, per direct user request outside this feature's scope, an unrelated manually-created `devin.udy@gmail.com` test account and confirmed it had no attached data.
- **2026-07-30** — Forgot Password completed on `feature/forgot-password`. Added password-reset support to `src/lib/db/verification.ts` (`createPasswordResetToken()`, `checkPasswordResetToken()`, `consumePasswordResetToken()`) reusing the existing `VerificationToken` model with a `"reset:" + email` identifier prefix — a distinct namespace from the plain-email identifier used by email verification, so a pending verify token and a pending reset token for the same user can never stomp each other, and consuming a reset token never touches `emailVerified`. Reset tokens are shorter-lived than email-verify tokens (1h vs 24h). Added `findResetEligibleUser()` (`src/lib/db/user.ts`), returning `null` for both nonexistent and GitHub-only (no `password` set) accounts so the caller can silently skip both. Added `src/lib/email/send-password-reset-email.ts` (mirrors `send-verification-email.ts`). Added `forgotPasswordSchema`/`resetPasswordSchema` to `src/lib/validations/auth.ts`. Added two server actions to `src/actions/auth.ts`: `requestPasswordReset()` (always returns a generic `{success: true}` regardless of whether the account exists/is eligible — no enumeration via response body) and `resetPassword()` (validates + consumes the token, hashes with `bcryptjs` at 12 rounds, redirects to `/sign-in?reset=success`). Added `/forgot-password` (page + `ForgotPasswordForm`, `useActionState`-driven, shows a "check your email" panel on submit) and `/reset-password?token=` (server component pre-checking token validity via `checkPasswordResetToken()` to decide between an error state — mirroring `/verify-email`'s expired/invalid pattern — or rendering `ResetPasswordForm`). `SignInForm` gained a "Forgot password?" link next to the password label and a success banner when landing via `?reset=success`. No schema/migration changes. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright + Neon MCP pass covering the full flow: request → generic success message identical for an existing eligible account, a nonexistent email, and (by code inspection, not a live OAuth account) a GitHub-only account; reset token correctly namespaced and coexisting alongside an unrelated email-verify token for the same address; valid token → password changed, old password rejected, new password signs in successfully; token single-use (revisit after consuming → "Invalid link"); unrecognized token → "Invalid link"; expired token (manually backdated in Neon) → "Link expired". Noted but not fixed (consistent with existing codebase conventions, no other auth endpoint has it either): `requestPasswordReset()` has a timing side-channel — eligible accounts trigger a DB write plus a Resend network call before returning, ineligible ones return immediately — a response-time-based enumeration vector distinct from the response-body protection this feature implements. Test account and its tokens deleted from the dev branch afterward via Neon MCP with explicit user confirmation.
- **2026-08-17** — Profile Page completed on `feature/profile-page` per `context/features/profile-spec.md`. Extended `getCurrentUser()` (`src/lib/db/user.ts`) with `createdAt` and a derived `hasPassword` boolean (the raw hash is destructured off and never returned). Added `getProfileStats()` (`src/lib/db/profile.ts`, `cache()`-wrapped like the dashboard queries) returning total items, total collections, and a per-type breakdown zero-filled across all 7 system types via `getSystemItemTypes()`. Added `src/actions/profile.ts`: `changePassword()` (bcrypt-verifies the current password, rejects GitHub-only accounts with no password set) and `deleteAccount()` (requires a literal `"DELETE"` confirmation, deletes the `User` row — cascading to items/collections/sessions/accounts via existing FK `onDelete: Cascade` — then calls NextAuth's `signOut`). Added matching Zod schemas in `src/lib/validations/profile.ts`. Added a `formatDate()` helper (`src/lib/format.ts`) for the absolute "Joined ..." date. Installed the shadcn `alert-dialog` component (Base UI-backed, matching the existing `dropdown-menu`/`alert` components); confirmed via Context7 that Base UI's `AlertDialog` has no `Action` primitive (only `Close`), so the shadcn `AlertDialogAction` is just a styled `Button` with no auto-close behavior — used that to keep `DeleteAccountDialog` open on a server-side error instead of dismissing prematurely. Built `ChangePasswordForm`, `DeleteAccountDialog` (client-side gates the submit button until the input exactly matches `DELETE`, mirroring the server-side check), and `ItemTypeBreakdown` (icon/color per type via the existing `getItemTypeIcon()`/inline `style={{color}}` convention from `CollectionCard`). Rewrote `/profile` as an async server component composing all of the above; `ChangePasswordForm` only renders when `hasPassword` is true. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a thorough live Playwright + Neon MCP pass: real demo-user stats/breakdown (18 items, 5 collections, correct per-type counts) render correctly; wrong-current-password error; a full change-password round trip (success message, then reverted back to the original password, confirmed by re-signing-in with it afterward) so the demo account's credentials were left unchanged; a freshly registered user shows the correct empty state and an initials-fallback avatar; the delete-confirmation dialog's submit button stays disabled until `DELETE` is typed exactly (case-sensitive) and Cancel closes without side effects; a full real delete on the throwaway account redirected to `/`, cleared the session (`/profile` re-gated to `/sign-in`), and was confirmed via a direct Neon query that the `User` row was actually gone.
- **2026-08-20** — Rate Limiting for Auth completed on `feature/rate-limiting-auth` per `context/features/rate-limiting-spec.md`. Installed `@upstash/ratelimit` + `@upstash/redis`. Added `src/lib/rate-limit.ts`: four sliding-window `Ratelimit` instances (login 5/15min IP+email, register 3/1hr IP, forgot-password 3/1hr IP, reset-password 5/15min IP) sharing one `Redis` client that's `null` when `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are unset; `checkRateLimit()` fails open (allows the request) both when unconfigured and when the Upstash call itself throws; `getClientIp()` reads `x-forwarded-for` via `next/headers`; `rateLimitMessage()`/`retryAfterSeconds()` format the shared "Too many attempts..." copy and header value. Wired into `POST /api/auth/register` (429 + `Retry-After` header, checked before body parsing) and three `src/actions/auth.ts` server actions — `signInWithCredentials` (keyed `${ip}:${email}`), `requestPasswordReset`, `resetPassword` (both IP-only) — each returning `{error}` into their existing `useActionState` shape, so the pre-existing `Alert`-based error UI in `SignInForm`/`ForgotPasswordForm`/`ResetPasswordForm`/`register/page.tsx` surfaced the new messages with zero component changes. Two spec-vs-codebase mismatches were resolved with the user before implementation: `resend-verification` (no such feature exists) was dropped as out of scope rather than built just to have something to rate-limit, and the spec's literal "15 hours" reset-password window was treated as a typo for 15 minutes. Documented the env vars in `.env.example` (untracked by git, matching this repo's existing `.env*` gitignore convention). Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and live testing against the user's already-configured Upstash instance: 4 rapid `POST /api/auth/register` calls (fake mismatched-password payloads, no DB writes) returned 400/400/400/429 with a `Retry-After` header and correct minutes-remaining message; 4 rapid Playwright submissions of `/forgot-password` (nonexistent emails, no emails sent) showed the normal enumeration-safe success message 3 times then the rate-limit error inline on the 4th. No test data left in Neon.
- **2026-08-21** — Three-Column Items Grid completed on `feature/three-column-items-grid`. One-line change to `/items/[type]` (`src/app/items/[type]/page.tsx:37`): grid class went from `grid-cols-1 gap-4 md:grid-cols-2` to `grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3`, so the layout now progresses 1 → 2 (md) → 3 (lg) columns instead of capping at 2. No component changes needed — `ItemCard` was already grid-agnostic. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, and a live Playwright pass on `/items/links` (6 seeded items) at three widths: 414px mobile (1 col), 900px tablet (2 cols), 1440px desktop (3 cols, correctly wraps into 2 rows).
- **2026-08-21** — Item Drawer Edit Mode completed on `feature/item-drawer-edit` per `context/features/item-drawer-edit-spec.md`. The item drawer's Edit button (previously disabled) now toggles the drawer into an inline edit form, replacing the action bar with Cancel/Save. Editable for all types: Title (required), Description, Tags (comma-separated → array on save); type-specific: Content (snippet/prompt/command/note), Language (snippet/command), URL (link). Item type, collections, and created/updated dates stay read-only in edit mode. Added `updateItem(itemId, data)` to `src/actions/items.ts` (session + Zod + ownership checks, delegates to the DB layer) and `updateItem(userId, itemId, data)` to `src/lib/db/items.ts` (tags: `deleteMany` all existing `ItemTag` rows then `connectOrCreate` new ones scoped to the user's `Tag` rows; returns the full `ItemDetail`). Extracted a shared `ITEM_DETAIL_SELECT`/`toItemDetail()` out of `getItemDetail` so both functions stay in sync. Added `src/lib/validations/items.ts` (`updateItemSchema`, matching the spec's field-by-field rules). This repo had no toast primitive yet, so installed shadcn's `sonner` (`src/components/ui/sonner.tsx`, wired into `src/app/layout.tsx` with `theme="dark"` since the app is dark-only) and `textarea` (`src/components/ui/textarea.tsx`) components via the shadcn CLI. `ItemDrawer.tsx` gained `mode`/`editForm` state; resetting them on item-id change uses the render-time "adjust state when a prop changes" pattern (`if (openItemId !== lastOpenItemId) { setLastOpenItemId(...); setMode("view"); ... }`) rather than `useEffect`, since ESLint's `react-hooks/set-state-in-effect` rule (new in this repo's React 19 setup) flags synchronous `setState` inside effects. Added 5 Vitest unit tests for `updateItem` to `src/actions/items.test.ts` (unauthorized, empty-title validation, invalid-URL validation, ownership failure, success path), mocking the new `@/lib/db/items` import alongside the existing `@/auth`/`@/lib/prisma` mocks; the DB-layer `updateItem` itself was left untested, consistent with this repo's convention of not unit-testing thin Prisma-query functions (`getItemDetail`, `getItemsByType`, etc.). Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (30/30 passing), and a live Playwright + Neon MCP pass: edited a Snippet's description/tags → toast → drawer and the underlying card list both updated without a manual reload (`router.refresh()`); Save disabled with an empty title; Cancel discarded an in-progress edit without touching a prior save; a Link item's edit form showed URL but no Content/Language; a Note item's edit form showed Content but no Language/URL. Reverted the test edit back to the original seed values afterward and confirmed via a direct Neon query — no lingering test data. Delete remains disabled (out of scope, unchanged from the original Item Drawer feature).
- **2026-08-21** — Item Delete completed on `feature/item-delete`. The item drawer's Delete button (previously disabled) now opens a shadcn `AlertDialog` confirming the destructive, irreversible action before anything happens. Added `deleteItem(itemId)` to `src/actions/items.ts` (session + ownership-scoped `findFirst` check, following the exact pattern of `toggleItemFavorite`/`toggleItemPinned`/`updateItem`) and `deleteItem(userId, itemId)` to `src/lib/db/items.ts` (thin `prisma.item.delete`, left unit-tested only at the action layer per this repo's convention). `ItemDrawer.tsx` gained `deleteDialogOpen` state (reset alongside `mode`/`editForm` on item-id change, consistent with the existing pattern) and a `handleDelete` handler; on success it closes the confirmation dialog, calls `toast.success("Item deleted")`, `close()`s the drawer, and `router.refresh()`s to sync server-rendered lists — mirroring `handleSave`. Added 3 Vitest unit tests for `deleteItem` (unauthorized, ownership failure, success) to `src/actions/items.test.ts`. During review, double-checked (beyond the initial live pass, which only used a bare item) that deleting an item with tags and a collection link cascades cleanly via the existing `onDelete: Cascade` FKs on `ItemTag`/`ItemCollection` — confirmed live via a direct Neon insert/delete that no orphaned join rows remain. Verified with `tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` (33/33 passing), and a live Playwright + Neon MCP pass: Delete → confirmation dialog naming the item → Cancel left the item untouched and the drawer open in view mode; Delete → confirm → drawer closed, "Item deleted" toast shown, item count dropped and the card disappeared from `/items/notes` with no manual reload, and the row was confirmed gone from Neon; no console errors. Test items cleaned up from the dev branch afterward.
